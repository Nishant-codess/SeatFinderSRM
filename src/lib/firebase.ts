// Firebase has been replaced by AWS (DynamoDB + Cognito).
// This file is kept as a stub so any remaining imports fail loudly at build time
// rather than silently at runtime.
//
// If you see build errors pointing here, search for the import and update it to:
//   import { docClient, TABLES } from "@/lib/aws";        (for database)
//   import { ... } from "@/lib/cognito-auth";             (for auth, client-side)
//   import { verifyAdmin, verifyUser } from "@/lib/verify-admin"; (for API routes)

export const app  = null;
export const auth = null;
export const db   = null;
